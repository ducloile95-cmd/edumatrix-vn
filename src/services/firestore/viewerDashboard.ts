import{collection,doc,getDoc,getDocs,limit,query,where}from"firebase/firestore";import{addDays,subDays}from"date-fns";import{db}from"@/services/firebase/client";import{COLLECTIONS}from"@/constants/collections";import{getStudent}from"@/services/firestore/students";import{listSessionsByClass}from"@/services/firestore/sessions";import{listPublicLessonPlansByClass}from"@/services/firestore/lessonPlans";import{listAssignmentsByClass,listSubmissionsByStudents}from"@/services/firestore/assignments";import{listScoresByStudent}from"@/services/firestore/scores";import{listAttendanceByStudents}from"@/services/firestore/attendance";import{listInvoicesByStudents}from"@/services/firestore/invoices";
export interface ViewerDashboardData{studentIds:string[];nextSessions:Record<string,unknown>[];lessonPlans:Record<string,unknown>[];pendingAssignments:Record<string,unknown>[];latestScores:Record<string,unknown>[];attendance:Record<string,unknown>[];unpaidInvoices:Record<string,unknown>[];announcements:Record<string,unknown>[];updatedAt:unknown;}
export async function getViewerDashboard(uid:string):Promise<ViewerDashboardData|null>{const snap=await getDoc(doc(db,COLLECTIONS.VIEWER_DASHBOARDS,uid));return snap.exists()?(snap.data()as ViewerDashboardData):null;}
export async function buildViewerDashboard(studentIds:string[]):Promise<ViewerDashboardData>{const students=await Promise.all(studentIds.map(getStudent));const classIds=[...new Set(students.flatMap(s=>s?.currentClassIds??[]))];const[sessionGroups,lessonGroups,assignmentGroups,submissions,scoreGroups,attendance,invoices,announcementGroups]=await Promise.all([Promise.all(classIds.map(id=>listSessionsByClass(id,subDays(new Date(),1),addDays(new Date(),30)))),Promise.all(classIds.map(listPublicLessonPlansByClass)),Promise.all(classIds.map(listAssignmentsByClass)),listSubmissionsByStudents(studentIds),Promise.all(studentIds.map(listScoresByStudent)),listAttendanceByStudents(studentIds),listInvoicesByStudents(studentIds),Promise.all(studentIds.map(async studentId=>{const snap=await getDocs(query(collection(db,COLLECTIONS.ANNOUNCEMENTS),where("studentId","==",studentId),limit(50)));return snap.docs.map(i=>({id:i.id,...i.data()}));}))]);const submitted=new Set(submissions.map(s=>s.assignmentId));return{studentIds,nextSessions:sessionGroups.flat().slice(0,10)as unknown as Record<string,unknown>[],lessonPlans:lessonGroups.flat().slice(0,10)as unknown as Record<string,unknown>[],pendingAssignments:assignmentGroups.flat().filter(a=>!submitted.has(String(a.id))).slice(0,10)as unknown as Record<string,unknown>[],latestScores:scoreGroups.flat().slice(0,10)as unknown as Record<string,unknown>[],attendance:attendance.slice(0,10)as unknown as Record<string,unknown>[],unpaidInvoices:invoices.filter(i=>i.status!=="paid").slice(0,10)as unknown as Record<string,unknown>[],announcements:announcementGroups.flat().slice(0,20),updatedAt:null};}

import { useMemo, useState } from "react"; import { useMutation,useQuery } from "@tanstack/react-query"; import { LineChart,Line,XAxis,YAxis,Tooltip,ResponsiveContainer } from "recharts"; import { AppShell } from "@/components/layouts/AppShell"; import { useAuth } from "@/features/auth/hooks/useAuth"; import { listClasses,getClass } from "@/services/firestore/classes"; import { listStudents } from "@/services/firestore/students"; import { listScoresByClass,saveClassScores,type ScoreEntry } from "@/services/firestore/scores"; import type { AssessmentType } from "@/types/academic";
export default function ScoresPage(){const{firebaseUser}=useAuth();const[classId,setClassId]=useState("");const[meta,setMeta]=useState({name:"",type:"quiz" as AssessmentType,max:10,subjectId:""});const[entries,setEntries]=useState<Record<string,ScoreEntry>>({});const classes=useQuery({queryKey:["classes"],queryFn:listClasses});const klass=useQuery({queryKey:["class",classId],queryFn:()=>getClass(classId),enabled:!!classId});const students=useQuery({queryKey:["students"],queryFn:listStudents});const scores=useQuery({queryKey:["scores",classId],queryFn:()=>listScoresByClass(classId),enabled:!!classId});const classStudents=students.data?.filter((s)=>klass.data?.studentIds.includes(s.id))??[];const save=useMutation({mutationFn:()=>saveClassScores({classId,subjectId:meta.subjectId,assessmentName:meta.name,assessmentType:meta.type,maxScore:Number(meta.max),entries:Object.values(entries),actorUid:firebaseUser?.uid??"unknown"}),onSuccess:()=>scores.refetch()});const chart=useMemo(()=>scores.data?.map((item)=>({name:item.assessmentName,value:Math.round(item.score/item.maxScore*100)})).slice(-20)??[],[scores.data]);return <AppShell><h1>Diem hoc tap</h1><div className="mt-5 flex flex-wrap gap-2"><select aria-label="Chon lop" value={classId} onChange={(e)=>setClassId(e.target.value)} className="min-h-touch rounded-input border px-3"><option value="">Chon lop</option>{classes.data?.map((item)=><option key={item.id} value={item.id}>{item.name}</option>)}</select><input aria-label="Ten bai danh gia" placeholder="Ten bai danh gia" value={meta.name} onChange={(e)=>setMeta({...meta,name:e.target.value})} className="min-h-touch rounded-input border px-3"/><select aria-label="Loai danh gia" value={meta.type} onChange={(e)=>setMeta({...meta,type:e.target.value as AssessmentType})} className="min-h-touch rounded-input border px-3"><option value="quiz">Quiz</option><option value="midterm">Giua ky</option><option value="final">Cuoi ky</option><option value="assignment">Bai tap</option></select><input aria-label="Ma mon" placeholder="Ma mon" value={meta.subjectId} onChange={(e)=>setMeta({...meta,subjectId:e.target.value})} className="min-h-touch rounded-input border px-3"/><input aria-label="Diem toi da" type="number" min={1} value={meta.max} onChange={(e)=>setMeta({...meta,max:Number(e.target.value)})} className="w-24 min-h-touch rounded-input border px-3"/></div><ul className="mt-4 divide-y">{classStudents.map((student)=><li key={student.id} className="grid gap-2 py-3 md:grid-cols-[1fr_120px_1fr]"><span className="text-sm font-medium">{student.fullName}</span><input aria-label={`Diem ${student.fullName}`} type="number" min={0} max={meta.max} value={entries[student.id]?.score??""} onChange={(e)=>setEntries({...entries,[student.id]:{studentId:student.id,score:Number(e.target.value),comment:entries[student.id]?.comment??""}})} className="min-h-touch rounded-input border px-3"/><input aria-label={`Nhan xet ${student.fullName}`} placeholder="Nhan xet" value={entries[student.id]?.comment??""} onChange={(e)=>setEntries({...entries,[student.id]:{studentId:student.id,score:entries[student.id]?.score??0,comment:e.target.value}})} className="min-h-touch rounded-input border px-3"/></li>)}</ul>{classStudents.length>0&&<button onClick={()=>save.mutate()} disabled={save.isPending} className="mt-3 min-h-touch rounded-input bg-primary-500 px-5 text-white disabled:opacity-50">{save.isPending?"Dang luu...":"Luu diem ca lop"}</button>}{save.isError&&<p role="alert" className="mt-2 text-sm text-danger-700">Luu that bai. Kiem tra mang va bam Luu lai - diem da nhap van con.</p>}{save.isSuccess&&<p className="mt-2 text-sm text-success-700">Da luu diem ca lop.</p>}<section className="mt-7"><h2>Lich su va tien bo</h2><div className="mt-3 h-64"><ResponsiveContainer width="100%" height="100%"><LineChart data={chart}><XAxis dataKey="name"/><YAxis domain={[0,100]}/><Tooltip/><Line type="monotone" dataKey="value" stroke="#3366F0" strokeWidth={2}/></LineChart></ResponsiveContainer></div></section></AppShell>}

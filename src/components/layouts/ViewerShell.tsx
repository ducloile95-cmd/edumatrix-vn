import { AppShell } from "@/components/layouts/AppShell";

/** Viewer dùng cùng AppShell với Staff; AppShell tự cấu hình điều hướng theo role. */
export function ViewerShell() {
  return <AppShell />;
}
